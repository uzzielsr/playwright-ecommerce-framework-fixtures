/**
 * PDP page locators.
 * These may change depending on the current ENV (e.g., qa, uat, prod).
 */
export const pdpLocators = {
    addToCartButton: 'button[class="btn btn-default cart"]',
    viewCartLink: 'a[href="/view_cart"]:has-text("View Cart")',
};